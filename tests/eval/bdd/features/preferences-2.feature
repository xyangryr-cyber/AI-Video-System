# AUTOGENERATED from docs/BDD_ai_system_expected_behavior_v1.feature.md
# Do not edit by hand; amend the source .feature.md and re-run
# scripts/split_bdd_scenarios.py --apply

@preferences @scoping
Feature: 偏好提取的全局与阶段作用域

  Scenario: AI 应区分全局偏好与阶段偏好
    Given 用户在本轮交互中表达“以后所有项目都不要学术腔，但这个项目的旁白语速稍慢一点”
    When PreferenceExtractor 提取候选偏好
    Then 应至少生成一条全局偏好候选和一条阶段偏好候选
    And 全局偏好应适用于所有阶段消费
    And 阶段偏好应限定在 TTS 或当前音频相关阶段消费

  Scenario: 阶段偏好不应错误污染其他阶段
    Given 用户只在音频阶段确认了“语速 0.92 倍、情绪更沉稳”的阶段偏好
    When 后续 ScriptAgent 或 StoryboardAgent 被调用
    Then 这些 Agent 不应消费该音频阶段专属偏好
    And TTSAgent 才应读取该阶段偏好

  Scenario: 偏好确认卡片应允许用户调整保存范围
    Given PreferenceExtractor 识别出一条“数据必须标注原始来源”的候选偏好
    When 用户打开偏好确认卡片
    Then 用户应可以把该偏好保存为全局偏好或仅本项目偏好
    And 确认后的偏好 scope 应被持久化并在后续调用中按 scope 注入
